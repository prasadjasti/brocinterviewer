{
  "name": "brocinterviewer-bot",
  "version": "1.0.0",
  "description": "A bot using node SDK to take the first round of interview ",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "botbuilder": "^3.8.4",
    "restify": "^5.0.0"
  }
}
